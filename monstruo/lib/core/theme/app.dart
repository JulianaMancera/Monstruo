import 'package:flutter/material.dart';

class MonstruoApp extends StatelessWidget {
  const MonstruoApp({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Monstruo'),
      ),
    );
  }
}
