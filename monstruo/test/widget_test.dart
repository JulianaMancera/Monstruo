import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('Monstruo smoke test placeholder', (WidgetTester tester) async {
    // Placeholder test — Hive requires initialization before the app can run.
    // Add integration tests here once the app is set up.
    expect(true, isTrue);
  });
}